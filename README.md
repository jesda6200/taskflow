# TaskFlow

Monorepo TaskFlow avec:
- `apps/backend`: API Express + TypeScript + Prisma
- `apps/frontend`: UI React + TypeScript + Vite + Tailwind
- `packages/shared`: Types partages

## Demarrage rapide

```bash
pnpm install
pnpm -r test
pnpm -r dev
```
